---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Documentation">
  <div class="max-w-4xl mx-auto px-6 py-12">
    <h1 class="text-4xl font-bold mb-8">Documentation</h1>

    <!-- Installation -->
    <section class="mb-12">
      <h2 class="text-2xl font-semibold mb-4" id="installation">Installation</h2>
      <div class="bg-surface-raised rounded-lg p-4 font-mono text-sm">
        <span class="text-code-comment"># npm</span><br />
        <span class="text-code-command">npm install</span> -D @sgc/cli<br /><br />
        <span class="text-code-comment"># pnpm</span><br />
        <span class="text-code-command">pnpm add</span> -D @sgc/cli<br /><br />
        <span class="text-code-comment"># yarn</span><br />
        <span class="text-code-command">yarn add</span> -D @sgc/cli
      </div>
    </section>

    <!-- Configuration -->
    <section class="mb-12">
      <h2 class="text-2xl font-semibold mb-4" id="configuration">Configuration</h2>
      <p class="text-text-muted mb-4">
        SGC uses the same configuration format as graphql-codegen. Create a <code class="bg-surface-inset px-2 py-1 rounded">codegen.config.ts</code> file in your project root:
      </p>
      <div class="bg-surface-raised rounded-lg p-4 font-mono text-sm overflow-x-auto">
        <pre class="text-text-secondary"><span class="text-code-keyword">import</span> type {'{'} CodegenConfig {'}'} <span class="text-code-keyword">from</span> <span class="text-code-string">'@sgc/cli'</span>;

<span class="text-code-keyword">const</span> config: CodegenConfig = {'{'}
  schema: <span class="text-code-string">'./schema.graphql'</span>,
  documents: <span class="text-code-string">'src/**/*.tsx'</span>,
  generates: {'{'}
    <span class="text-code-string">'./src/generated/types.ts'</span>: {'{'}
      plugins: [<span class="text-code-string">'typescript'</span>, <span class="text-code-string">'typescript-operations'</span>]
    {'}'}
  {'}'}
{'}'};

<span class="text-code-keyword">export default</span> config;</pre>
      </div>
    </section>

    <!-- Schema -->
    <section class="mb-12">
      <h2 class="text-2xl font-semibold mb-4" id="schema">Schema Sources</h2>
      <p class="text-text-muted mb-4">
        The <code class="bg-surface-inset px-2 py-1 rounded">schema</code> option accepts:
      </p>
      <ul class="list-disc list-inside text-text-muted space-y-2 mb-4">
        <li>A path to a <code class="bg-surface-inset px-1 rounded">.graphql</code> file</li>
        <li>A glob pattern matching multiple schema files</li>
        <li>An array of paths or patterns</li>
      </ul>
      <div class="bg-surface-raised rounded-lg p-4 font-mono text-sm">
        <pre class="text-text-secondary"><span class="text-code-comment">// Single file</span>
schema: <span class="text-code-string">'./schema.graphql'</span>

<span class="text-code-comment">// Multiple files</span>
schema: [
  <span class="text-code-string">'./schema/*.graphql'</span>,
  <span class="text-code-string">'./extensions/**/*.graphql'</span>
]</pre>
      </div>
    </section>

    <!-- Documents -->
    <section class="mb-12">
      <h2 class="text-2xl font-semibold mb-4" id="documents">Documents</h2>
      <p class="text-text-muted mb-4">
        The <code class="bg-surface-inset px-2 py-1 rounded">documents</code> option specifies where to find your GraphQL operations (queries, mutations, subscriptions, fragments):
      </p>
      <div class="bg-surface-raised rounded-lg p-4 font-mono text-sm">
        <pre class="text-text-secondary"><span class="text-code-comment">// Find operations in TypeScript/JavaScript files</span>
documents: <span class="text-code-string">'src/**/*.tsx'</span>

<span class="text-code-comment">// Multiple patterns</span>
documents: [
  <span class="text-code-string">'src/**/*.tsx'</span>,
  <span class="text-code-string">'src/**/*.ts'</span>,
  <span class="text-code-string">'!src/generated/**'</span>  <span class="text-code-comment">// Exclude generated files</span>
]</pre>
      </div>
    </section>

    <!-- Plugins -->
    <section class="mb-12">
      <h2 class="text-2xl font-semibold mb-4" id="plugins">Plugins</h2>
      <p class="text-text-muted mb-4">
        SGC supports the following plugins:
      </p>
      <div class="space-y-4">
        <div class="bg-surface-raised rounded-lg p-4">
          <h3 class="font-semibold text-text-heading mb-2">typescript</h3>
          <p class="text-text-muted text-sm">
            Generates TypeScript types for your GraphQL schema (scalars, enums, input types, object types).
          </p>
        </div>
        <div class="bg-surface-raised rounded-lg p-4">
          <h3 class="font-semibold text-text-heading mb-2">typescript-operations</h3>
          <p class="text-text-muted text-sm">
            Generates TypeScript types for your GraphQL operations (queries, mutations, subscriptions) and fragments.
          </p>
        </div>
      </div>
    </section>

    <!-- Plugin Options -->
    <section class="mb-12">
      <h2 class="text-2xl font-semibold mb-4" id="plugin-options">Plugin Options</h2>
      <p class="text-text-muted mb-4">
        Configure plugin behavior with the <code class="bg-surface-inset px-2 py-1 rounded">config</code> option:
      </p>
      <div class="bg-surface-raised rounded-lg p-4 font-mono text-sm overflow-x-auto">
        <pre class="text-text-secondary">generates: {'{'}
  <span class="text-code-string">'./src/generated/types.ts'</span>: {'{'}
    plugins: [<span class="text-code-string">'typescript'</span>, <span class="text-code-string">'typescript-operations'</span>],
    config: {'{'}
      <span class="text-code-comment">// Use 'type' instead of 'interface'</span>
      declarationKind: <span class="text-code-string">'type'</span>,

      <span class="text-code-comment">// Make optional fields use T | undefined instead of Maybe&lt;T&gt;</span>
      maybeValue: <span class="text-code-string">'T | undefined'</span>,

      <span class="text-code-comment">// Skip __typename in output</span>
      skipTypename: <span class="text-code-value">true</span>,

      <span class="text-code-comment">// Custom scalar mappings</span>
      scalars: {'{'}
        DateTime: <span class="text-code-string">'string'</span>,
        JSON: <span class="text-code-string">'Record&lt;string, unknown&gt;'</span>
      {'}'}
    {'}'}
  {'}'}
{'}'}</pre>
      </div>

      <h3 class="text-xl font-semibold mt-8 mb-4">Available Options</h3>
      <div class="overflow-x-auto">
        <table class="w-full text-sm">
          <thead>
            <tr class="border-b border-border-default">
              <th class="text-left py-3 px-4 text-text-muted">Option</th>
              <th class="text-left py-3 px-4 text-text-muted">Type</th>
              <th class="text-left py-3 px-4 text-text-muted">Default</th>
              <th class="text-left py-3 px-4 text-text-muted">Description</th>
            </tr>
          </thead>
          <tbody class="text-text-secondary">
            <tr class="border-b border-border-default/50">
              <td class="py-3 px-4 font-mono text-code-string">declarationKind</td>
              <td class="py-3 px-4 font-mono">'type' | 'interface'</td>
              <td class="py-3 px-4 font-mono">'type'</td>
              <td class="py-3 px-4">Output as type aliases or interfaces</td>
            </tr>
            <tr class="border-b border-border-default/50">
              <td class="py-3 px-4 font-mono text-code-string">skipTypename</td>
              <td class="py-3 px-4 font-mono">boolean</td>
              <td class="py-3 px-4 font-mono">false</td>
              <td class="py-3 px-4">Omit __typename from generated types</td>
            </tr>
            <tr class="border-b border-border-default/50">
              <td class="py-3 px-4 font-mono text-code-string">maybeValue</td>
              <td class="py-3 px-4 font-mono">string</td>
              <td class="py-3 px-4 font-mono">'T | null'</td>
              <td class="py-3 px-4">Template for nullable types</td>
            </tr>
            <tr class="border-b border-border-default/50">
              <td class="py-3 px-4 font-mono text-code-string">scalars</td>
              <td class="py-3 px-4 font-mono">Record&lt;string, string&gt;</td>
              <td class="py-3 px-4 font-mono">{'{}'}</td>
              <td class="py-3 px-4">Map custom scalars to TypeScript types</td>
            </tr>
            <tr class="border-b border-border-default/50">
              <td class="py-3 px-4 font-mono text-code-string">enumsAsTypes</td>
              <td class="py-3 px-4 font-mono">boolean</td>
              <td class="py-3 px-4 font-mono">false</td>
              <td class="py-3 px-4">Generate enums as union types</td>
            </tr>
          </tbody>
        </table>
      </div>
    </section>

    <!-- CLI Usage -->
    <section class="mb-12">
      <h2 class="text-2xl font-semibold mb-4" id="cli">CLI Usage</h2>
      <div class="bg-surface-raised rounded-lg p-4 font-mono text-sm">
        <pre class="text-text-secondary"><span class="text-code-comment"># Run with default config (codegen.config.ts)</span>
<span class="text-code-command">npx sgc</span>

<span class="text-code-comment"># Specify config file</span>
<span class="text-code-command">npx sgc</span> -c ./my-config.ts

<span class="text-code-comment"># Watch mode</span>
<span class="text-code-command">npx sgc</span> --watch

<span class="text-code-comment"># Skip cache (force regeneration)</span>
<span class="text-code-command">npx sgc</span> --no-cache

<span class="text-code-comment"># Show timing information</span>
<span class="text-code-command">npx sgc</span> --timing</pre>
      </div>
    </section>

    <!-- Migration -->
    <section class="mb-12">
      <h2 class="text-2xl font-semibold mb-4" id="migration">Migrating from graphql-codegen</h2>
      <p class="text-text-muted mb-4">
        SGC is designed as a drop-in replacement. In most cases, you can:
      </p>
      <ol class="list-decimal list-inside text-text-muted space-y-2 mb-4">
        <li>Install <code class="bg-surface-inset px-1 rounded">@sgc/cli</code></li>
        <li>Keep your existing <code class="bg-surface-inset px-1 rounded">codegen.ts</code> or <code class="bg-surface-inset px-1 rounded">codegen.yml</code></li>
        <li>Replace <code class="bg-surface-inset px-1 rounded">graphql-codegen</code> with <code class="bg-surface-inset px-1 rounded">sgc</code> in your scripts</li>
      </ol>
      <div class="bg-surface-raised rounded-lg p-4 font-mono text-sm">
        <pre class="text-text-secondary"><span class="text-code-comment">// package.json</span>
{'{'}
  "scripts": {'{'}
    <span class="text-red-400 line-through">"codegen": "graphql-codegen"</span>
    <span class="text-code-command">"codegen": "sgc"</span>
  {'}'}
{'}'}</pre>
      </div>
    </section>
  </div>
</Layout>
