query GetUser($id: ID!) {
  user(id: $id) {
    id
    name
    email
  }
}

query GetUsers {
  users {
    id
    name
  }
}

mutation CreateUser($input: CreateUserInput!) {
  createUser(input: $input) {
    id
    name
  }
}
